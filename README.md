# 115网盘 .strm 生成脚本

一键生成 115 网盘的 strm 文件，支持 emby/jellyfin 串流播放，并可同步更新 alist 索引数据库。

## 快速开始

```bash
sudo bash -c "$(curl -fsSL https://raw.githubusercontent.com/holddyxu/115-strm/refs/heads/main/115-strm.sh)"
```

## ✨ 功能特性

- 📂 **目录树转换** - 将 115 目录树转换为目录文件
- 🎬 **生成 strm 文件** - 支持音频、视频、图片等 62+ 种格式
- 📊 **alist 索引数据库** - 同步更新 alist 搜索索引
- 🤖 **Telegram Bot** - 通过 TG 远程控制脚本（新增）
- ⚙️ **自动更新脚本** - 定时自动更新 strm 文件
- 🚀 **后台服务** - 支持 systemd 开机自启

---

## 🤖 Telegram Bot 功能（新增）

通过 Telegram 远程控制 115-strm 脚本，无需 SSH 登录服务器。

### 配置步骤

1. **创建 Bot**
   - 在 Telegram 搜索 @BotFather
   - 发送 `/newbot` 创建机器人
   - 保存获得的 Token

2. **获取 Chat ID**
   - 搜索 @userinfobot
   - 发送任意消息获取你的 User ID

3. **配置 bot.conf**
   ```bash
   # 编辑配置文件
   vim bot.conf
   ```
   ```ini
   TG_BOT_TOKEN="你的Bot Token"
   TG_CHAT_ID="你的群组/个人Chat ID"
   TG_ALLOWED_USERS="允许使用的用户ID（空格分隔）"
   TG_MSG_PREFIX="[115-strm]"
   ```

4. **启动 Bot**
   ```bash
   ./tg_bot.sh
   ```

### TG Bot 命令

| 命令 | 说明 |
|------|------|
| `/start` | 显示欢迎信息 |
| `/menu` | 显示功能菜单 |
| `/convert` | 目录树转换 |
| `/strm` | 生成 strm 文件 |
| `/index` | 更新 alist 索引数据库 |
| `/status` | 查看当前配置 |
| `/cancel` | 取消当前操作 |

---

## 🚀 服务安装（开机自启 + 后台运行）

将 TG Bot 安装为系统服务：

```bash
# 安装服务
chmod +x install_service.sh
sudo bash install_service.sh
```

### 服务管理命令

```bash
sudo systemctl start 115-strm-bot    # 启动
sudo systemctl stop 115-strm-bot     # 停止
sudo systemctl restart 115-strm-bot  # 重启
sudo systemctl status 115-strm-bot   # 状态
sudo journalctl -u 115-strm-bot -f   # 日志
```

---

## 📖 使用教程

### 1️⃣ 生成目录树

在 115 网盘中，长按要处理的文件夹 → 生成目录树 → 下载

教程：https://115.com/115115/T496626.html

### 2️⃣ 将目录树转换为目录文件

运行脚本选择 `1`，输入目录树文件路径或 alist 下载链接

### 3️⃣ 生成 strm 文件

运行脚本选择 `2`，依次输入：
- strm 保存路径
- alist 地址（如 `http://192.168.1.100:5244`）
- 挂载路径（如 `/115`）
- 剔除层级（默认 2）

### 4️⃣ 更新 alist 索引数据库

运行脚本选择 `3`，输入：
- alist 的 data.db 文件路径
- 挂载路径
- 剔除层级
- 选择新增或替换

> ⚠️ **重要**：操作前请备份 data.db 文件

---

## ⚙️ 性能优化

| 处理阶段 | 优化前 (10000文件) | 优化后 | 提升 |
|---------|-------------------|--------|------|
| 目录解析 | 1200ms | 800ms | ↓33% |
| 路径过滤 | 500ms | 0ms | ✅跳过 |
| 总处理时间 | 18s | 9s | ↓50% |

### 格式选择

```
1. 音频 [15种]  2. 视频 [10种]
3. 图片 [8种]   4. 其他 [19种]
5. 全选（默认） 0. 自定义扩展名
```

---

## 📋 环境要求

- 系统：Ubuntu 20.04+ / Debian 10+
- 依赖：`python3`、`sqlite3`、`curl`、`iconv`
- alist 版本：v3.37.x 以上
- **使用 strm 需关闭 alist 签名**

### 关闭 alist 签名

1. 管理 → 设置 → 全局 → 关闭签名所有
2. 存储 → 对应存储 → 启用签名 → 关闭

---

## 📁 文件说明

| 文件 | 说明 |
|------|------|
| `115-strm.sh` | 主脚本 |
| `tg_bot.sh` | Telegram Bot 服务 |
| `bot.conf` | TG Bot 配置文件 |
| `install_service.sh` | systemd 服务安装脚本 |

---

## 🔗 相关链接

- Docker 版：https://github.com/uwang/115-strm
- 音流播放器：https://github.com/gitbobobo/StreamMusic
- iOS 推荐：Fileball（支持 alist 搜索 API）

---

## 📞 联系方式

- Telegram: https://t.me/suixing6

## 🙏 致谢

- ChatGPT-4o
- Deepseek
- [@uwang](https://github.com/uwang)
- 原作者 [@suixing8](https://github.com/suixing8)
